options {
        listen-on port 53 { 127.0.0.1; <%= @ipaddress %>; };
        directory       "/var/named";
        dump-file       "/var/named/data/cache_dump.db";
        statistics-file "/var/named/data/named_stats.txt";
        memstatistics-file "/var/named/data/named_mem_stats.txt";
        allow-query { <% for @ip in @allowquery %><%= @ip %> <% end %> };
        recursion yes;
        forwarders { <% for @ip in  @forwarders %><%= @ip %> <% end %> };
        allow-recursion { any; };

};

logging {
        channel default_debug {
                file "data/named.run";
                severity dynamic;
        };
};

zone "." IN {
        type hint;
        file "named.ca";
};

zone "<%= @zone_name %>" IN {
        type master;
        file "<%= @zone_name %>.zone";
        notify no;
};

include "/etc/named.rfc1912.zones";
include "/etc/named.root.key";
