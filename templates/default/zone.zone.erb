$TTL   <%= @ttl %>
@   IN  SOA   <%= @zone_name %>.  root.<%= @zone_name %>. (
                      <%= @serial %>      ; Serial
                      <%= @refresh %>     ; refresh
                      <%= @retry %>       ; retry
                      <%= @expire %>      ; expire
                      <%= @minimum %> )   ; minimum
;
; NS records
<% @nsrecords.each do |nsrecord| %>
<%= nsrecord %>
<% end %>
;
; A records
<% @arecords.each do |arecord| %>
<%= arecord %>
<% end %>
